# Example env file
# For development (used by `pnpm start:dev`):
REACT_APP_GRAPHQL_URL=http://localhost:4000/graphql

# For production (used by `pnpm start:prod` after you set a value):
# REACT_APP_GRAPHQL_URL=https://your-production-graphql.example.com/graphql
